///Lectura e impresion de strings
#include <bits/stdc++.h>
using namespace std;
long long int numerote;

int main() {
    ios_base::sync_with_stdio(0); cin.tie(0);
    cin >> numerote;
    cout << to_string(numerote).size();
    return 0;
}
